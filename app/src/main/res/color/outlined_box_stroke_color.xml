<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Active state color (when the field is focused) -->
    <item android:color="@color/myYellow" android:state_focused="true" />
    <!-- Inactive state color (when the field is not focused) -->
    <item android:color="@color/white"/>
</selector>
